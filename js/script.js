jQuery(document).ready(function($){
  
	var container = document.querySelector('#lec-misc-list');
	var msnry = new Masonry( container, {
	// options

	itemSelector: '.item',
	gutter: 50
	});
})

jQuery(document).ready(function($){
  
	var container = document.querySelector('#cookening-misc-list');
	var msnry = new Masonry( container, {
	// options

	itemSelector: '.item',
	gutter: 50
	});
})